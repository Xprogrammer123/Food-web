module.exports = {
    plugins: {
      '@tailwindcss/postcss': {}, // Use the new Tailwind CSS PostCSS plugin
      autoprefixer: {},          // Optional but recommended for vendor prefixing
    },
  };